# OKVEJ Telegram Bot v13.1

Исправленная чистая сборка.

- `/version` всегда показывает реальные BOT_VERSION и BOT_BUILD;
- администратор определяется только через ADMIN_USER_ID;
- `/admin_debug` удалена;
- новинки и хиты выбираются вручную из карточки товара;
- категории определяются по первому слову;
- данные хранятся в одном `admin_data.json`;
- каталог, поиск, корзина, избранное и история сохранены.

Railway:
- ADMIN_USER_ID=210563345
- ADMIN_DATA_PATH=/data/admin_data.json
