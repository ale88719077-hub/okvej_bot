# OKVEJ Telegram Bot v6.1

Исправлена ошибка Telegram `reply markup is too long`.

- категории выводятся по 12 на страницу;
- добавлены кнопки перехода между страницами категорий;
- товары в наличии: `presence.id == 1`;
- категории определяются по URL товара;
- поиск, корзина, оформление заказа и `/пост` сохранены;
- `/version` показывает 6.1.
