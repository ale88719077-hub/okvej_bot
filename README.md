# OKVEJ Telegram Bot v6.2

## Исправлено

- каждый товар больше не становится отдельной категорией;
- категория берётся из API, из родительского сегмента URL или определяется по названию товара;
- неизвестный slug товара не используется как категория;
- категории выводятся по 12 на страницу;
- каталог показывает только товары с `presence.id == 1`;
- `/version` показывает 6.2;
- поиск, корзина, оформление заказа и `/пост` сохранены.
