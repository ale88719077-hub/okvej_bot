# OKVEJ Telegram Bot v6

## Новое в v6

- исправлено определение категорий;
- если Horoshop API не возвращает `category`, категория берётся из URL товара;
- известные URL-категории преобразуются в нормальные украинские названия;
- каталог показывает только товары с `presence.id == 1`;
- категории с количеством товаров;
- пагинация по 8 товаров;
- кнопка обновления каталога;
- команда `/version` показывает версию 6.0;
- поиск, корзина, оформление заказа и `/пост` сохранены.
